/** @file PickMode.ts */

/**
 * Defines different modes for picking.
 */
const enum PickMode
{
	/** The method used for picking as pointer input. */
	PointerInput = 0,

	/** The method used for picking to ignore pointer input settings. */
	HitTest = 1
}
