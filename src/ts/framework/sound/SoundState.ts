/** @file SoundState.ts */

/**
 * Enumeration of the sound playback states.
 */
const enum SoundState
{
	/** The sound is stopped. */
	Stopped = 0,

	/** The sound is playing. */
	Playing = 1,

	/** The sound is paused. */
	Paused = 2
}
